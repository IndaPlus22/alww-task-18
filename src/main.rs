use make_magic::magic;
use traits::DasHash;

mod lib_search;
mod make_magic;
mod traits;
fn main() {
    magic();
    // println!("{}", "a".hash());
    // println!("{}", "torogarano".hash());
    // println!("{}", "c".hash());
    // println!("{}", "d".hash());
    // println!("{}", "e".hash());
}
